{{- range $key, $value := .Values }}
  {{- with $value.host }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $key }}-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: {{ default "avi-ingress-class-helsenett" $value.ingress.classname }}
  tls:
  - hosts:
    - {{ $value.host }}
    secretName: {{ $key }}-tls-secret
  rules:
  - host: {{ $value.host }}
    http:
      paths:
      - pathType: Prefix
        path: "/api"
        backend:
          service:
            name: api-service
            port:
              number: 8080
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: web-service
            port:
              number: 8080
---
{{- end }}
{{- end }}