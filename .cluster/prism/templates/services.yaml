apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-service
spec:
  type: {{ .Values.prism.serviceType | default "ClusterIP" }}
  selector:
    app: {{ .Chart.Name }}
  ports:
  - name: http
    port: 8080
    targetPort: 8080
